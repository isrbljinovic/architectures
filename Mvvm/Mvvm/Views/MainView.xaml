<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utility="clr-namespace:Mvvm.Utility"
             x:Name="Main"
             Title="Firma"
             utility:ViewModelLocator.AutoWireViewModel="True"
             x:Class="Mvvm.Views.MainView">

    <StackLayout Padding="50">
        <Button Text="Pregled dokumenata" Command="{Binding DokumentiViewCommand}" Background="LightGray"/>
        <Button Text="Novi dokument" Command="{Binding CreateDokumentViewCommand}" Background="LightGray"/>
    </StackLayout>

</ContentPage>
